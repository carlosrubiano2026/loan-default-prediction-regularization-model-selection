# loan-default-prediction-regularization-model-selection
Credit-risk classification project predicting loan default using regularized models, tree-based methods, leakage diagnostics, and model selection.
